# CLAUDE.md

芳賀胃腸科内科クリニックのWebサイト（Nuxt 3 + Nuxt Content + UnoCSS）。

記事コンテンツは `content/` 配下の Markdown で管理しており、Nuxt Studio から編集されることもある。

## お知らせ（content/news/）のファイル名規則

新しいお知らせを追加するときは、必ず次の形式にする。

```
YYMMDD-内容を表す英小文字.md

例）260916-influenza-vaccination.md  →  /news/260916-influenza-vaccination
```

- 日付6桁（YYMMDD）+ **ハイフン** + 内容を表す英語スラッグ
- 英字は**小文字のみ**（大文字を含めるとURLは小文字化され、ファイル名とURLが食い違う）
- 単語の区切りもハイフン
- 日付は frontmatter の `date` と一致させる

frontmatter の形式:

```yaml
---
title: 【お知らせ】インフルエンザワクチン接種を開始しました
date: 2026-09-16T00:00:00.000Z
img: ''
---
```

### ドット区切り（`260916.influenza-vaccination.md`）にしてはいけない理由

Nuxt Content は「数字 + ドット」で始まるファイル名の先頭を*並び順指定用のプレフィックス*と解釈し、**URLから取り除く**。そのため日付が消えて `/news/influenza-vaccination` になる。

結果、毎年繰り返すお知らせ（インフルエンザ予防接種、春季・夏季休暇、年末年始など）は翌年の記事とURLが衝突し、**新しい記事が表示されなくなる**。ハイフン区切りなら日付がURLに残るため衝突しない。

### 既存ファイルを勝手にリネームしないこと

`content/news/` にはドット区切りの既存ファイルが残っているが、**リネームすると公開中のURLが変わって404になる**ため、このリポジトリでは意図的に据え置いている。一括リネームはリダイレクト設定（`routeRules`）とセットで別途対応する（Issue #9 の段階2）。

新規追加分にだけ上記の規則を適用する。

### 規則に反するファイル名を見つけたら指摘する

`content/news/` に新しく追加されようとしているファイル名が上の規則に沿っていない場合は、作業を進める前にその旨を指摘すること。

## content/ の他のディレクトリについて

`inspections/` `conditions/` `doctors/` のファイル名にある `100.` `01.` などの数字プレフィックスは、**表示順を制御するために意図的に使っている**。これらは個別ページのルートを持たず（一覧内で描画されるだけ）URLに現れないため、上のハイフン規則は適用しない。**リネームしないこと。**

## 開発

```bash
npm install
npm run dev      # http://localhost:3000
npm run build
```
